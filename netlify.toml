[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Avoid optional platform packages from local Windows installs
  NPM_FLAGS = "--include=dev"
